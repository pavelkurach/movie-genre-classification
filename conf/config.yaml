defaults:
  - training_arguments: local
  - _self_

pretrained_model_name: distilbert-base-uncased
short_model_name: distilbert
num_genres: 10

seed: 42

mlflow_uri: https://localhost:5000
mlflow_model_path: models/distilbert-base-uncased
mlflow_experiment_name: Movie genre classification